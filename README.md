# README

This is how we build clusters in each of the clouds.

## Destroy

To destroy a cluster, add `-destroy` to the `tofu plan` and `tofu apply` lines in the `.github/workflows/opentofu.yml` file.
